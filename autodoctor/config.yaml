name: "AutoDoctor"
version: "0.1.0"
slug: "autodoctor"
description: >-
  Watches Home Assistant system log events, deduplicates incidents, asks an AI
  to investigate actionable failures, and prepares safe repair proposals.
url: "https://github.com/jacobsscoots/Home-Assistant"
startup: application
boot: auto
init: false
arch:
  - amd64
  - aarch64
homeassistant_api: true
hassio_api: true
hassio_role: homeassistant
ingress: true
ingress_port: 8099
panel_icon: mdi:doctor
panel_title: AutoDoctor
options:
  min_level: "ERROR"
  min_occurrences_for_ai: 2
  analysis_cooldown_seconds: 900
  max_ai_analyses_per_hour: 12
  ai_provider: "none"
  ai_model: ""
  ai_effort: "low"
  openai_api_key: ""
  anthropic_api_key: ""
  mcp_enabled: false
  mcp_url: ""
  mcp_token: ""
  notify_on_new_incident: true
  auto_apply_low_risk: false
schema:
  min_level: "list(WARNING|ERROR)"
  min_occurrences_for_ai: "int(1,20)"
  analysis_cooldown_seconds: "int(60,86400)"
  max_ai_analyses_per_hour: "int(1,100)"
  ai_provider: "list(none|openai|anthropic)"
  ai_model: str
  ai_effort: "list(low|medium|high)"
  openai_api_key: password
  anthropic_api_key: password
  mcp_enabled: bool
  mcp_url: str
  mcp_token: password
  notify_on_new_incident: bool
  auto_apply_low_risk: bool
